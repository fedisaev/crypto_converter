export interface CryptoData {
    name: string;
    fullName: string;
    imageUrl: string;
    price: number;
}

export interface Coin {
    name: string;
    fullName: string;
    imageUrl: string;
}